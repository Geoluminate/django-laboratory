============
Installation
============

At the command line::

    $ easy_install django-laboratory

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-laboratory
    $ pip install django-laboratory
